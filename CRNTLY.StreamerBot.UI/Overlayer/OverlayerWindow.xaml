<Window x:Class="Crntly.StreamerBot.UI.Overlayer.OverlayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="CRNTLY • Overlayer"
        Width="780" Height="440" MinWidth="700" MinHeight="390"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResize"
        Background="{DynamicResource Crntly.Background}">

  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/CRNTLY.StreamerBot.UI;component/Theme/CrntlyTheme.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>

  <shell:WindowChrome.WindowChrome>
    <shell:WindowChrome CaptionHeight="0"
                        ResizeBorderThickness="4"
                        CornerRadius="3"
                        GlassFrameThickness="0" />
  </shell:WindowChrome.WindowChrome>

  <Border Background="{DynamicResource Crntly.Background}"
          BorderBrush="{DynamicResource Crntly.Border}"
          BorderThickness="1"
          CornerRadius="{DynamicResource Crntly.WindowRadius}">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="{DynamicResource Crntly.TitleBarHeight}" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="16" />
      </Grid.RowDefinitions>

      <!-- compact desktop title bar -->
      <Border Grid.Row="0"
              Background="{DynamicResource Crntly.Surface}"
              BorderBrush="{DynamicResource Crntly.Border}"
              BorderThickness="0,0,0,1"
              CornerRadius="3,3,0,0"
              MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
        <Grid Margin="7,0,3,0">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="24" />
            <ColumnDefinition Width="24" />
          </Grid.ColumnDefinitions>

          <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock Text="CRNTLY" Foreground="{DynamicResource Crntly.TextMuted}"
                       FontWeight="SemiBold" FontSize="9" VerticalAlignment="Center" />
            <TextBlock Text="  /  Overlayer" Foreground="{DynamicResource Crntly.Text}"
                       FontWeight="SemiBold" FontSize="10" VerticalAlignment="Center" />
          </StackPanel>

          <StackPanel Grid.Column="1" Orientation="Horizontal"
                      Margin="0,0,6,0" VerticalAlignment="Center">
            <Ellipse x:Name="ServerDot" Width="5" Height="5"
                     Fill="{DynamicResource Crntly.TextMuted}" Margin="0,0,5,0" />
            <TextBlock x:Name="ServerBadgeText" Text="SERVER OFFLINE"
                       Foreground="{DynamicResource Crntly.TextMuted}"
                       FontSize="8" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Border x:Name="ServerBadge" Width="0" Height="0" Visibility="Collapsed" />
          </StackPanel>

          <Button Grid.Column="2" Content="—"
                  Style="{StaticResource Crntly.TitleButton}"
                  Click="Minimize_Click"
                  shell:WindowChrome.IsHitTestVisibleInChrome="True" />
          <Button Grid.Column="3" Content="×"
                  Style="{StaticResource Crntly.TitleButton}"
                  Click="Close_Click"
                  shell:WindowChrome.IsHitTestVisibleInChrome="True" />
        </Grid>
      </Border>

      <!-- compositor connection -->
      <Border Grid.Row="1" Style="{StaticResource Crntly.Card}"
              Margin="7,5,7,4" Padding="5">
        <Grid>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="6" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4" />
            <ColumnDefinition Width="22" />
            <ColumnDefinition Width="2" />
            <ColumnDefinition Width="22" />
          </Grid.ColumnDefinitions>

          <Button x:Name="ServerButton" Grid.Column="0" Content="Start server"
                  Style="{StaticResource Crntly.PrimaryButton}"
                  Click="ServerButton_Click" MinWidth="84" />

          <TextBox x:Name="ServerUrlBox" Grid.Column="2" IsReadOnly="True"
                   Text="http://localhost:42069/"
                   VerticalContentAlignment="Center" />

          <Button Grid.Column="4"
                  Style="{StaticResource Crntly.IconButton}"
                  Click="CopyUrl_Click"
                  ToolTip="Copy compositor URL">
            <Path Style="{StaticResource Crntly.IconPath}"
                  Data="M19,21H8C6.9,21 6,20.1 6,19V8C6,6.9 6.9,6 8,6H19C20.1,6 21,6.9 21,8V19C21,20.1 20.1,21 19,21 M16,3H5C3.9,3 3,3.9 3,5V16H5V5H16V3Z" />
          </Button>

          <Button x:Name="OpenPreviewButton" Grid.Column="6"
                  Style="{StaticResource Crntly.IconButton}"
                  Click="OpenPreview_Click"
                  ToolTip="Open compositor preview in browser">
            <Path Style="{StaticResource Crntly.IconPath}"
                  Data="M14,3V5H17.59L7.76,14.83L9.17,16.24L19,6.41V10H21V3H14 M19,19H5V5H12V3H5C3.9,3 3,3.9 3,5V19C3,20.1 3.9,21 5,21H19C20.1,21 21,20.1 21,19V12H19V19Z" />
          </Button>
        </Grid>
      </Border>

      <!-- workspace -->
      <Grid Grid.Row="2" Margin="7,0,7,4">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="225" />
          <ColumnDefinition Width="6" />
          <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- overlay list -->
        <Border Grid.Column="0" Style="{StaticResource Crntly.Card}" Padding="6">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="25" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>

              <TextBlock Text="Overlays" Foreground="{DynamicResource Crntly.Text}"
                         FontWeight="SemiBold" FontSize="11"
                         VerticalAlignment="Center" />

              <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Style="{StaticResource Crntly.IconButton}"
                        Click="Add_Click" ToolTip="Add overlay">
                  <Path Style="{StaticResource Crntly.IconPath}"
                        Data="M11,5H13V11H19V13H13V19H11V13H5V11H11V5Z" />
                </Button>
                <Button x:Name="DuplicateButton"
                        Style="{StaticResource Crntly.IconButton}"
                        Click="Duplicate_Click" ToolTip="Duplicate selected overlay">
                  <Path Style="{StaticResource Crntly.IconPath}"
                        Data="M19,21H8C6.9,21 6,20.1 6,19V8C6,6.9 6.9,6 8,6H19C20.1,6 21,6.9 21,8V19C21,20.1 20.1,21 19,21 M16,3H5C3.9,3 3,3.9 3,5V16H5V5H16V3Z" />
                </Button>
                <Border Style="{StaticResource Crntly.ToolbarSeparator}" />
                <Button x:Name="MoveUpButton"
                        Style="{StaticResource Crntly.IconButton}"
                        Click="MoveUp_Click" ToolTip="Move overlay up">
                  <Path Style="{StaticResource Crntly.IconPath}"
                        Data="M7,14L12,9L17,14H7Z" />
                </Button>
                <Button x:Name="MoveDownButton"
                        Style="{StaticResource Crntly.IconButton}"
                        Click="MoveDown_Click" ToolTip="Move overlay down">
                  <Path Style="{StaticResource Crntly.IconPath}"
                        Data="M7,10L12,15L17,10H7Z" />
                </Button>
                <Button x:Name="DeleteButton"
                        Style="{StaticResource Crntly.DangerIconButton}"
                        Click="Delete_Click" ToolTip="Delete selected overlay">
                  <Path Style="{StaticResource Crntly.IconPath}"
                        Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19V4 M6,19C6,20.1 6.9,21 8,21H16C17.1,21 18,20.1 18,19V7H6V19Z" />
                </Button>
              </StackPanel>
            </Grid>

            <ListBox Grid.Row="1" x:Name="OverlayList"
                     SelectionChanged="OverlayList_SelectionChanged">
              <ListBox.ItemTemplate>
                <DataTemplate>
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="30" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <CheckBox Grid.Column="0"
                              IsChecked="{Binding Enabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                              Checked="OverlayEnabledChanged" Unchecked="OverlayEnabledChanged"
                              VerticalAlignment="Center" />

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                      <TextBlock Text="{Binding Name}" Foreground="{DynamicResource Crntly.Text}"
                                 FontWeight="SemiBold" FontSize="10"
                                 TextTrimming="CharacterEllipsis" />
                      <TextBlock Text="{Binding Url}"
                                 Foreground="{DynamicResource Crntly.TextSubtle}"
                                 FontSize="8" Margin="0,1,5,0"
                                 TextTrimming="CharacterEllipsis" />
                    </StackPanel>

                    <TextBlock Grid.Column="2" Text="{Binding SourceKind}"
                               Foreground="{DynamicResource Crntly.TextSubtle}"
                               FontSize="7" FontWeight="SemiBold"
                               VerticalAlignment="Top" Margin="3,1,0,0" />
                  </Grid>
                </DataTemplate>
              </ListBox.ItemTemplate>
            </ListBox>
          </Grid>
        </Border>

        <!-- editor -->
        <Border Grid.Column="2" Style="{StaticResource Crntly.Card}" Padding="7">
          <ScrollViewer VerticalScrollBarVisibility="Auto"
                        HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="0,0,2,0">
              <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                  <TextBlock Text="Overlay settings"
                             Foreground="{DynamicResource Crntly.Text}"
                             FontWeight="SemiBold" FontSize="12" />
                  <StackPanel Orientation="Horizontal" Margin="7,0,0,0" VerticalAlignment="Center">
                    <Ellipse x:Name="EditorStatusDot" Width="4" Height="4"
                             Fill="{DynamicResource Crntly.TextMuted}" Margin="0,0,4,0" />
                    <TextBlock x:Name="EditorStatusText" Text="Autosave"
                               Foreground="{DynamicResource Crntly.TextMuted}"
                               FontSize="8" />
                  </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                  <Button x:Name="ResetLayoutButton"
                          Style="{StaticResource Crntly.IconButton}"
                          Click="ResetLayout_Click"
                          Margin="0,0,5,0"
                          ToolTip="Reset size and position to defaults">
                    <Path Style="{StaticResource Crntly.IconPath}"
                          Data="M12,5V2L8,6L12,10V7C15.31,7 18,9.69 18,13C18,16.31 15.31,19 12,19C8.69,19 6,16.31 6,13H4C4,17.42 7.58,21 12,21C16.42,21 20,17.42 20,13C20,8.58 16.42,5 12,5Z" />
                  </Button>
                  <CheckBox x:Name="EnabledBox" Content="Enabled"
                            VerticalAlignment="Center"
                            Checked="EditorEnabledChanged"
                            Unchecked="EditorEnabledChanged" />
                </StackPanel>
              </Grid>

              <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="0.34*" />
                  <ColumnDefinition Width="6" />
                  <ColumnDefinition Width="0.66*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                  <TextBlock Text="NAME" Style="{StaticResource Crntly.Label}" Margin="0,0,0,2" />
                  <TextBox x:Name="NameBox" TextChanged="EditorField_TextChanged" />
                </StackPanel>

                <StackPanel Grid.Column="2">
                  <TextBlock Text="URL / LOCAL FILE" Style="{StaticResource Crntly.Label}" Margin="0,0,0,2" />
                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="3" />
                      <ColumnDefinition Width="22" />
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="UrlBox" TextChanged="EditorField_TextChanged" />
                    <Button x:Name="OpenSourceButton" Grid.Column="2"
                            Style="{StaticResource Crntly.IconButton}"
                            Click="OpenSource_Click"
                            ToolTip="Open overlay source in browser">
                      <Path Style="{StaticResource Crntly.IconPath}"
                            Data="M14,3V5H17.59L7.76,14.83L9.17,16.24L19,6.41V10H21V3H14 M19,19H5V5H12V3H5C3.9,3 3,3.9 3,5V19C3,20.1 3.9,21 5,21H19C20.1,21 21,20.1 21,19V12H19V19Z" />
                    </Button>
                  </Grid>
                </StackPanel>
              </Grid>

              <Grid Margin="0,0,0,6">
                <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="*" />
                  <ColumnDefinition Width="6" />
                  <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                  <TextBlock Text="WIDTH" Style="{StaticResource Crntly.Label}" Margin="0,0,0,2" />
                  <TextBox x:Name="WidthBox" TextChanged="EditorField_TextChanged" />
                </StackPanel>

                <StackPanel Grid.Column="2">
                  <TextBlock Text="HEIGHT" Style="{StaticResource Crntly.Label}" Margin="0,0,0,2" />
                  <TextBox x:Name="HeightBox" TextChanged="EditorField_TextChanged" />
                </StackPanel>
              </Grid>

              <Border Style="{StaticResource Crntly.SubtleCard}" Padding="6">
                <StackPanel>
                  <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="Position" Foreground="{DynamicResource Crntly.Text}"
                               FontWeight="SemiBold" FontSize="10" />
                    <TextBlock Text="  ·  live"
                               Foreground="{DynamicResource Crntly.TextSubtle}"
                               FontSize="8" VerticalAlignment="Center" />
                  </StackPanel>

                  <Grid Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="14" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="6" />
                      <ColumnDefinition Width="58" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="X"
                               Foreground="{DynamicResource Crntly.TextMuted}"
                               FontWeight="SemiBold" FontSize="9"
                               VerticalAlignment="Center" />
                    <Slider Grid.Column="1" x:Name="LeftSlider"
                            Minimum="-1920" Maximum="3840"
                            SmallChange="1" LargeChange="50"
                            IsMoveToPointEnabled="True"
                            VerticalAlignment="Center"
                            ValueChanged="PositionSlider_ValueChanged"
                            ToolTip="X position in pixels. Range: -1920 to 3840." />
                    <TextBox Grid.Column="3" x:Name="LeftBox"
                             TextAlignment="Right"
                             TextChanged="EditorField_TextChanged" />
                  </Grid>

                  <Grid>
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="14" />
                      <ColumnDefinition Width="*" />
                      <ColumnDefinition Width="6" />
                      <ColumnDefinition Width="58" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Y"
                               Foreground="{DynamicResource Crntly.TextMuted}"
                               FontWeight="SemiBold" FontSize="9"
                               VerticalAlignment="Center" />
                    <Slider Grid.Column="1" x:Name="TopSlider"
                            Minimum="-1080" Maximum="2160"
                            SmallChange="1" LargeChange="50"
                            IsMoveToPointEnabled="True"
                            VerticalAlignment="Center"
                            ValueChanged="PositionSlider_ValueChanged"
                            ToolTip="Y position in pixels. Range: -1080 to 2160." />
                    <TextBox Grid.Column="3" x:Name="TopBox"
                             TextAlignment="Right"
                             TextChanged="EditorField_TextChanged" />
                  </Grid>
                </StackPanel>
              </Border>
            </StackPanel>
          </ScrollViewer>
        </Border>
      </Grid>

      <Grid Grid.Row="3" Margin="8,0">
        <TextBlock Text="CRNTLY • livestreaming.tools"
                   Foreground="{DynamicResource Crntly.TextSubtle}"
                   FontSize="7" VerticalAlignment="Center" />
        <TextBlock Text="single-source compositor"
                   Foreground="{DynamicResource Crntly.TextSubtle}"
                   FontSize="7" HorizontalAlignment="Right"
                   VerticalAlignment="Center" />
      </Grid>
    </Grid>
  </Border>
</Window>
